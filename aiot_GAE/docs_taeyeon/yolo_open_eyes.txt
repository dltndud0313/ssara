### 1. [내 컴퓨터 터미널] 권한 & 도커 켜기

Bash

`xhost +local:root
docker start isaac-lab`

### 2. [터미널 1] 필수 설치 & 시뮬레이터 켜기

**여기가 핵심입니다!** YOLO를 먼저 깔아야 합니다.

Bash

`docker exec -it isaac-lab bash

# (도커 내부 진입 후)
# 1. YOLO 라이브러리 설치 (딱 한 번만 하면 됨)
pip install ultralytics opencv-python

# 2. ROS 설정
source /opt/ros/humble/setup.bash

# 3. 시뮬레이터 실행 (폴더 이름 spot_micro 로 수정함!)
cd /workspace/IsaacLab
./isaaclab.sh -p my_projects/spot_micro/main.py --enable_cameras`

### 3. [터미널 2] YOLO 실행

Bash

`docker exec -it isaac-lab bash

# (도커 내부 진입 후)
source /opt/ros/humble/setup.bash

# (폴더 이름 spot_micro 로 수정!)
python3 /workspace/IsaacLab/my_projects/spot_micro/yolo_detect.py`